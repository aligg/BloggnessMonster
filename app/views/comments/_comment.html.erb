<strong>
      Posted <%= time_ago_in_words(comment.created_at) %> ago by <%=comment.commenter %>
    </strong>
    <p>
  
  <%= comment.body %>
</p>
<p>
  <%= link_to 'Delete Comment', [comment.article, comment],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
</p>